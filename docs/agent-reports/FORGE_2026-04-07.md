# Forge report — 2026-04-07

## What changed
- Replaced fixed queue/table/preview height defaults with viewport-aware `clamp(...)` values for Library, Review, Duplicates, Organize, and Downloads.
- Updated `UiPreferencesContext` so persisted panel sizes write responsive `max(savedPx, min(vh target, ceilingPx))` expressions instead of restoring laptop-sized hard caps.
- Reworked Settings layout ownership so the screen shell, main grid, and focus panel all use explicit full-height row contracts and scroll correctly.
- Widened and rebalanced the Home shell for large canvases, including stronger hero/module row allocation and better height distribution in module stacks.

## Shared patterns implemented
- Applied explicit full-height composition rules where they were missing: `height: 100%`, `min-height: 0`, and `auto + minmax(0, 1fr)` row splits.
- Treated persisted resize preferences as minimums rather than absolute caps, keeping user intent without breaking responsive scaling.
- Anchored internal panel content more deliberately with stretch/flex ownership and bottom-pinned action groups for taller inspector/detail rails.

## Pages improved most
- Settings and Home benefited most because their root composition contracts were corrected for large-canvas use instead of only filling the viewport superficially.
- Library, Review, Duplicates, Organize, Downloads, and Updates also improved because the main content regions now own vertical space instead of inheriting short fixed caps.

## Follow-up needed if any
- Re-verify saved custom panel sizes and key breakpoints around 1100px, 1440p, and 4K/ultrawide.
- The old desktop Selenium viewport audit is still stale against current selectors; keep using the in-project Playwright capture path until that script is repaired.
